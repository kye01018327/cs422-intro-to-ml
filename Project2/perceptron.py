# %%
import numpy as np
from utils import load_data

# %%
num = 1
print(load_data(f'data_{num}.txt'))

# %%
def perceptron_train(X, Y):
    # Initialize weight vector and bias to 0
        # Initialize weight vector 
    w, b = 0, 0
    
    return w, b

# %%
def perceptron_test(X_test, Y_test, w, b):
    pass

